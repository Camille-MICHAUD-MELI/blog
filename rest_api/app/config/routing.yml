app:
    resource: '@AppBundle/Controller/'
    type: annotation

users:
    type:     rest
    resource: AppBundle\Controller\UserController

message:
    type:     rest
    resource:  AppBundle\Controller\MessageController

comment:
    type:     rest
    resource:  AppBundle\Controller\CommentController

fav:
    type:     rest
    resource:  AppBundle\Controller\FavController